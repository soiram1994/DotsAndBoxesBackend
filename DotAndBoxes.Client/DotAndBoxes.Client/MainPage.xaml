<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:DotAndBoxes.Client"
             x:Class="DotAndBoxes.Client.MainPage">
    <ContentPage.BindingContext>
        <local:MainPageViewModel/>
    </ContentPage.BindingContext>
    <Grid>
        <Button IsEnabled="True" Text="Join" Command="{Binding JoinGame}" HorizontalOptions="Center" VerticalOptions="Center"/>
        <ListView ItemsSource="{Binding Dots}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Label Text="{Binding X}" Grid.Column="{Binding Y}" Grid.Row="{Binding X}"/>

                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

    

</ContentPage>
